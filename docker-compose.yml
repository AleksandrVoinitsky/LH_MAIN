services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:?Set POSTGRES_DB in .env}
      POSTGRES_USER: ${POSTGRES_USER:?Set POSTGRES_USER in .env}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready --username=$$POSTGRES_USER --dbname=$$POSTGRES_DB"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend-api:
    build:
      context: .
      dockerfile: docker/backend/Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__MainDb: Host=postgres;Port=5432;Database=${POSTGRES_DB:?Set POSTGRES_DB in .env};Username=${POSTGRES_USER:?Set POSTGRES_USER in .env};Password=${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      Authentication__JwtSigningKey: ${JWT_SIGNING_KEY:?Set JWT_SIGNING_KEY in .env}
      Authentication__Issuer: ${JWT_ISSUER:?Set JWT_ISSUER in .env}
      Authentication__Audience: ${JWT_AUDIENCE:?Set JWT_AUDIENCE in .env}
      Authentication__AccessTokenLifetimeMinutes: ${JWT_ACCESS_TOKEN_LIFETIME_MINUTES:?Set JWT_ACCESS_TOKEN_LIFETIME_MINUTES in .env}
      Authentication__EnableDevRegistration: "true"
      GameServers__SharedKey: ${GAME_SERVER_SHARED_KEY}
      GameServers__TicketLifetimeSeconds: ${GAME_SERVER_TICKET_LIFETIME_SECONDS}
      GameServers__Slots__0__ServerId: ${GAME_SERVER_1_ID}
      GameServers__Slots__0__PublicHost: ${GAME_SERVER_PUBLIC_HOST}
      GameServers__Slots__0__PublicPort: ${GAME_SERVER_1_NETWORK_HOST_PORT}
      GameServers__Slots__1__ServerId: ${GAME_SERVER_2_ID}
      GameServers__Slots__1__PublicHost: ${GAME_SERVER_PUBLIC_HOST}
      GameServers__Slots__1__PublicPort: ${GAME_SERVER_2_NETWORK_HOST_PORT}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:8080:8080"
    healthcheck:
      test: ["CMD-SHELL", "curl --fail --silent http://localhost:8080/health/ready || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 10

  game-server-1:
    profiles: ["game-servers"]
    build:
      context: .
      dockerfile: docker/game-server/Dockerfile
    environment:
      GAME_SERVER_ID: ${GAME_SERVER_1_ID:?Set GAME_SERVER_1_ID in .env}
      GAME_SERVER_HTTP_PORT: ${GAME_SERVER_HTTP_CONTAINER_PORT:?Set GAME_SERVER_HTTP_CONTAINER_PORT in .env}
      GAME_SERVER_NETWORK_PORT: ${GAME_SERVER_NETWORK_CONTAINER_PORT:?Set GAME_SERVER_NETWORK_CONTAINER_PORT in .env}
      GAME_SERVER_PUBLIC_HOST: ${GAME_SERVER_PUBLIC_HOST:?Set GAME_SERVER_PUBLIC_HOST in .env}
      GAME_SERVER_PUBLIC_NETWORK_PORT: ${GAME_SERVER_1_NETWORK_HOST_PORT:?Set GAME_SERVER_1_NETWORK_HOST_PORT in .env}
      GAME_SERVER_BACKEND_BASE_URL: ${GAME_SERVER_BACKEND_BASE_URL:?Set GAME_SERVER_BACKEND_BASE_URL in .env}
      GAME_SERVER_SHARED_KEY: ${GAME_SERVER_SHARED_KEY:?Set GAME_SERVER_SHARED_KEY in .env}
      GAME_SERVER_TICKET_VALIDATION_TIMEOUT_SECONDS: ${GAME_SERVER_TICKET_VALIDATION_TIMEOUT_SECONDS:?Set GAME_SERVER_TICKET_VALIDATION_TIMEOUT_SECONDS in .env}
    depends_on:
      backend-api:
        condition: service_healthy
    ports:
      - "127.0.0.1:${GAME_SERVER_1_HTTP_HOST_PORT:?Set GAME_SERVER_1_HTTP_HOST_PORT in .env}:${GAME_SERVER_HTTP_CONTAINER_PORT:?Set GAME_SERVER_HTTP_CONTAINER_PORT in .env}/tcp"
      - "127.0.0.1:${GAME_SERVER_1_NETWORK_HOST_PORT:?Set GAME_SERVER_1_NETWORK_HOST_PORT in .env}:${GAME_SERVER_NETWORK_CONTAINER_PORT:?Set GAME_SERVER_NETWORK_CONTAINER_PORT in .env}/udp"
    healthcheck:
      test: ["CMD-SHELL", "curl --fail --silent http://localhost:$${GAME_SERVER_HTTP_PORT}/health/ready || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 20

  game-server-2:
    profiles: ["game-servers"]
    build:
      context: .
      dockerfile: docker/game-server/Dockerfile
    environment:
      GAME_SERVER_ID: ${GAME_SERVER_2_ID:?Set GAME_SERVER_2_ID in .env}
      GAME_SERVER_HTTP_PORT: ${GAME_SERVER_HTTP_CONTAINER_PORT:?Set GAME_SERVER_HTTP_CONTAINER_PORT in .env}
      GAME_SERVER_NETWORK_PORT: ${GAME_SERVER_NETWORK_CONTAINER_PORT:?Set GAME_SERVER_NETWORK_CONTAINER_PORT in .env}
      GAME_SERVER_PUBLIC_HOST: ${GAME_SERVER_PUBLIC_HOST:?Set GAME_SERVER_PUBLIC_HOST in .env}
      GAME_SERVER_PUBLIC_NETWORK_PORT: ${GAME_SERVER_2_NETWORK_HOST_PORT:?Set GAME_SERVER_2_NETWORK_HOST_PORT in .env}
      GAME_SERVER_BACKEND_BASE_URL: ${GAME_SERVER_BACKEND_BASE_URL:?Set GAME_SERVER_BACKEND_BASE_URL in .env}
      GAME_SERVER_SHARED_KEY: ${GAME_SERVER_SHARED_KEY:?Set GAME_SERVER_SHARED_KEY in .env}
      GAME_SERVER_TICKET_VALIDATION_TIMEOUT_SECONDS: ${GAME_SERVER_TICKET_VALIDATION_TIMEOUT_SECONDS:?Set GAME_SERVER_TICKET_VALIDATION_TIMEOUT_SECONDS in .env}
    depends_on:
      backend-api:
        condition: service_healthy
    ports:
      - "127.0.0.1:${GAME_SERVER_2_HTTP_HOST_PORT:?Set GAME_SERVER_2_HTTP_HOST_PORT in .env}:${GAME_SERVER_HTTP_CONTAINER_PORT:?Set GAME_SERVER_HTTP_CONTAINER_PORT in .env}/tcp"
      - "127.0.0.1:${GAME_SERVER_2_NETWORK_HOST_PORT:?Set GAME_SERVER_2_NETWORK_HOST_PORT in .env}:${GAME_SERVER_NETWORK_CONTAINER_PORT:?Set GAME_SERVER_NETWORK_CONTAINER_PORT in .env}/udp"
    healthcheck:
      test: ["CMD-SHELL", "curl --fail --silent http://localhost:$${GAME_SERVER_HTTP_PORT}/health/ready || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 20

volumes:
  postgres-data:
